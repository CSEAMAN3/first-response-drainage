export type Nav = {
  href: string;
  title: string;
};

export type Faq = {
  id: number;
  question: string;
  answer: string;
  category: string;
};

export type Approach = {
  heading: string;
  paragraph: string;
};
